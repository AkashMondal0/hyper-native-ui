import React, { useEffect } from 'react'
// import { useTheme } from 'src/hooks/useTheme'

const ThemeProvider = () => {
    // const { setThemeColors } = useTheme()

    useEffect(() => {
        // setThemeColors("Zinc", "light")
    }, [])
    return <></>
}

export default ThemeProvider
