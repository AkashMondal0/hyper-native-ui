---
title: "Input"
publishedAt: "2024-12-30"
# image: "/images/gallery/img-02.jpg"
summary: "Open a Terminal in your project's folder and run"
tag: "Android IOS Web"
---

The `Input` component is a highly customizable text input field that integrates seamlessly into different design systems. It supports themes, variants, and sizes, providing flexibility for various use cases.
## Installation

* Open a Terminal in your project's folder and run:


<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `import { Input } from 'hyper-native-ui';`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
### Usage Component

<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Input placeholder="Enter text" />`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
---
### **Expo Example Usage:**
<div 
data-snack-id="@akashmondal0/input-hyper-native-ui" 
data-snack-platform="web" 
data-snack-preview="true" 
data-snack-theme="dark" 
style={{
  "overflow":"hidden",
  "background":"#0C0D0E",
  "border":"1px solid var(--color-border)",
  "borderRadius":"4px",
  "height":"505px",
  "width":"100%"
}}></div>
<script async src="https://snack.expo.dev/embed.js"></script>

## Props

### `lightColor`
- **Type**: `string`
- **Optional**
- **Description**: Custom background color for light mode.

### `darkColor`
- **Type**: `string`
- **Optional**
- **Description**: Custom background color for dark mode.

### `disabled`
- **Type**: `boolean`
- **Optional**
- **Description**: Disables the input field if set to `true`.

### `themeScheme`
- **Type**: `"light" | "dark"`
- **Optional**
- **Description**: Overrides the current theme scheme.

### `rightSideComponent`
- **Type**: `React.ReactNode`
- **Optional**
- **Description**: Component displayed on the right side of the input field.

### `leftSideComponent`
- **Type**: `React.ReactNode`
- **Optional**
- **Description**: Component displayed on the left side of the input field.

### `onFocus`
- **Type**: `() => void`
- **Optional**
- **Description**: Callback triggered when the input gains focus.

### `onBlur`
- **Type**: `() => void`
- **Optional**
- **Description**: Callback triggered when the input loses focus.

### `isErrorBorder`
- **Type**: `boolean | unknown`
- **Optional**
- **Description**: Displays an error border if set to `true`.

### `placeholder`
- **Type**: `string`
- **Optional**
- **Default**: `'Enter text here'`
- **Description**: Placeholder text displayed when the input is empty.

### `containerStyle`
- **Type**: `ViewProps["style"]`
- **Optional**
- **Description**: Style object for the container view.

### `variant`
- **Type**: `"default" | "secondary" | ThemeName`
- **Optional**
- **Default**: `"default"`
- **Description**: Determines the visual style of the input field. Variants include custom theme names.

### `size`
- **Type**: `"small" | "medium" | "large" | "extraLarge"`
- **Optional**
- **Default**: `"medium"`
- **Description**: Specifies the size of the input field.

### `...props`
- **Type**: `TextInputProps`
- **Optional**
- **Description**: Additional props passed to the underlying `TextInput` component.

## Behavior

The `Input` component adjusts its appearance based on the provided `variant`, `size`, and `themeScheme`. It uses the `useTheme` hook to dynamically apply theme colors and styles.

### Theme Color Resolution Logic
1. If the `variant` is `"default"` or `"secondary"`, current theme colors are used.
2. For custom variants (e.g., `"Red"`, `"Orange"`), matching colors from `themeColors` are applied.
3. Fallback colors are used when no matching theme or variant is found.

### Size Variants
- **`small`**: Smaller font size for compact inputs.
- **`medium`**: Default size.
- **`large`**: Larger font size for more prominent inputs.
- **`extraLarge`**: Extra-large font size for high-visibility inputs.

## Example Usage
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `import React from 'react';
import { View } from 'react-native';
import Input from './Input';

const App = () => {
  return (
    <View>
      {/* Default Input */}
      <Input
        placeholder="Default Input"
        variant="default"
        onFocus={() => console.log('Focused!')}
        onBlur={() => console.log('Blurred!')}
      />

      {/* Input with Error Border */}
      <Input
        placeholder="Error Input"
        isErrorBorder={true}
        variant="Red"
        size="large"
      />

      {/* Input with Custom Components */}
      <Input
        placeholder="Custom Components"
        leftSideComponent={<View style={{ width: 20, height: 20, backgroundColor: 'blue' }} />}
        rightSideComponent={<View style={{ width: 20, height: 20, backgroundColor: 'green' }} />}
      />
    </View>
  );
};

export default App;`,
            label: 'React Native',
            language: 'tsx'
        },
    ]}
    copyButton
/>