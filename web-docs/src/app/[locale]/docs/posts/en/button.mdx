---
title: "Button"
publishedAt: "2024-12-18"
# image: "/images/gallery/img-02.jpg"
summary: "A customizable Button component with support for themes, sizes, variants, icons, and loading states."
tag: "Android IOS Web"
---

The `Button` component is a versatile, theme-aware button for React Native applications. It supports various styles, sizes, states, and customization options such as loading indicators, icons, and theming.

Import Component

<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `import { useTheme, Button } from 'hyper-native-ui';`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
Usage Component

<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button onPress={toggleTheme}>This is a Button</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
---
### **Expo Example Usage:**
<div 
data-snack-id="@akashmondal0/button-hyper-native-ui" 
data-snack-platform="web" 
data-snack-preview="true" 
data-snack-theme="dark" 
style={{
  "overflow":"hidden",
  "background":"#0C0D0E",
  "border":"1px solid var(--color-border)",
  "borderRadius":"4px",
  "height":"505px",
  "width":"100%"
}}></div>
<script async src="https://snack.expo.dev/embed.js"></script>

### **Props**

The `Button` component accepts the following props:

- **`themeScheme`**  
  Sets the theme scheme explicitly. Can be one of `"light"` or `"dark"`.  
  **Type**: `"light" | "dark"`  
  **Default**: Inherited theme  

- **`children`**  
  Content of the button. Can be a `string` or any `React.ReactNode`.  
  **Type**: `string | React.ReactNode`  
  **Default**: `"Button"`  

- **`textStyle`**  
  Custom styles for the button text.  
  **Type**: `TextProps["style"]`  
  **Default**: `undefined`  

- **`textTextProps`**  
  Additional props for the `Text` component.  
  **Type**: `TextProps`  
  **Default**: `undefined`  

- **`loadingStyle`**  
  Custom styles for the loading spinner.  
  **Type**: `ActivityIndicatorProps["style"]`  
  **Default**: `undefined`  

- **`loadingProps`**  
  Additional props for the loading spinner.  
  **Type**: `ActivityIndicatorProps`  
  **Default**: `undefined`  

- **`size`**  
  Defines the size of the button. Can be one of `"small"`, `"medium"`, `"large"`, or `"extraLarge"`.  
  **Type**: `"small" | "medium" | "large" | "extraLarge"`  
  **Default**: `"medium"`  

- **`icon`**  
  Adds an optional icon to the button.  
  **Type**: `React.ReactNode`  
  **Default**: `undefined`  

- **`loading`**  
  When `true`, shows a loading spinner and disables interaction.  
  **Type**: `boolean`  
  **Default**: `false`  

- **`disableMemo`**  
  Disables memoization of the component for debugging purposes.  
  **Type**: `boolean`  
  **Default**: `false`  

- **`variant`**  
  Controls the button style variant. Can be one of `"default"`, `"secondary"`, `"danger"`, `"warning"`, `"success"`, `"outline"`, or a custom `ThemeName`.  
  **Type**: `"default" | "secondary" | "danger" | "warning" | "success" | "outline" | ThemeName`  
  **Default**: `"default"`  

- **`style`**  
  Custom styles for the button container.  
  **Type**: `TouchableOpacityProps["style"]`  
  **Default**: `undefined`  

- **`disabled`**  
  Disables the button interaction and applies a reduced opacity.  
  **Type**: `boolean`  
  **Default**: `false`  

- **`...otherProps`**  
  Additional props forwarded to the `TouchableOpacity` component.  
  **Type**: `TouchableOpacityProps`  
  **Default**: `undefined`  



### **Variants**

The `variant` prop controls the button's color scheme and styling. It supports the following predefined values:

- **`default`**: Uses the primary theme color.
- **`secondary`**: Uses the secondary theme color.
- **`danger`**: A red variant for destructive actions.
- **`warning`**: A yellow variant for warnings.
- **`success`**: A green variant for success actions.
- **`outline`**: A transparent background with a border matching the theme color.
- **Custom**: Pass a `ThemeName` for custom theming.

Example usage:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button variant="danger">Delete</Button>
<Button variant="success">Save</Button>
<Button variant="outline">Cancel</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>

---

### **Sizes**

The `size` prop controls the padding and text size of the button. Available options:

- **`small`**: Smaller padding and text size.
- **`medium`** (default): Standard size.
- **`large`**: Larger button with increased padding.
- **`extraLarge`**: Extra-large button with more padding.

Example usage:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button size="small">Small Button</Button>
<Button size="medium">Medium Button</Button>
<Button size="large">Large Button</Button>
<Button size="extraLarge">Extra Large Button</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
---

### **Loading State**

The `loading` prop shows a spinner and disables the button when set to `true`.

Example usage:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button loading={true}>Loading...</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>

You can customize the loading spinner's appearance using `loadingStyle` and `loadingProps` props.

---

### **Icons**

The `icon` prop allows you to include an icon next to the button text.

Example usage:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `import { Text, View } from "react-native";
import { Ionicons } from "@expo/vector-icons";

<Button icon={<Ionicons name="add-circle" size={20} color="white" />}>Add</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
---

### **Theming**

The `Button` component integrates with a theme using the `useTheme` hook. You can:

1. Set a theme scheme (`"light"` or `"dark"`) using the `themeScheme` prop.
2. Customize colors dynamically via the theme system.
3. Use predefined variants (e.g., `default`, `success`) or extend with `ThemeName` for custom themes.

Example usage:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button themeScheme="light" variant="default">Light Theme Button</Button>
<Button themeScheme="dark" variant="secondary">Dark Theme Button</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
---

### **Full Example**
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `import React from "react";
import { Button } from "./Button";
import { View } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function App() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Button
        variant="success"
        size="large"
        loading={false}
        icon={<Ionicons name="checkmark-circle" size={20} color="white" />}
      >
        Submit
      </Button>
      <Button variant="danger" loading={true} size="medium">
        Deleting...
      </Button>
      <Button variant="outline" size="small">
        Cancel
      </Button>
    </View>
  );
}`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>

---

### **Accessibility**

- The `TouchableOpacity` component ensures proper accessibility behaviors.
- You can add accessibility labels using `...otherProps` forwarded to the button.

Example:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button accessibilityLabel="Submit form button">Submit</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>
---

### **Custom Styles**

You can override styles for the button container and text:
<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `<Button style={{ backgroundColor: "purple", borderRadius: 20 }}>
  Custom Button
</Button>
<Button textStyle={{ fontSize: 20, color: "white" }}>Styled Text</Button>`,
            label: 'React Native',
            language: 'jsx'
        },
    ]}
    copyButton
/>

---

### **Dependencies**

- **React Native**: Base components like `TouchableOpacity`, `Text`, and `ActivityIndicator`.
- **`useTheme` Hook**: Required to manage the theme and colors.

## Component Code

<CodeBlock className="my-24"
    compact
    codeInstances={[
        {
            code: `import {
    Text,
    TouchableOpacity,
    ActivityIndicator,
    type TextProps,
    type TouchableOpacityProps,
    type ActivityIndicatorProps
} from 'react-native';
import React, { memo } from 'react';
import useTheme from '../hooks/useTheme';
import { componentVariant, themeColors, } from '../constants/Colors';

export type Props = TouchableOpacityProps & {
    themeScheme?: "light" | "dark";
    children?: string | React.ReactNode;
    textStyle?: TextProps["style"];
    textTextProps?: TextProps;
    loadingStyle?: ActivityIndicatorProps["style"];
    loadingProps?: ActivityIndicatorProps;
    size?: "small" | "medium" | "large" | "extraLarge";
    icon?: React.ReactNode;
    loading?: boolean;
    disableMemo?: boolean;
    variant?: componentVariant;
};

const Button = memo(function Button({
    children = "Button",
    style,
    themeScheme,
    textStyle,
    loadingStyle,
    loadingProps,
    textTextProps,
    variant = "default",
    size = "medium",
    icon = undefined,
    loading = false,
    disabled = false,
    // disableMemo is used to disable memoization of the component
    disableMemo = false,
    ...otherProps }: Props) {
    const { currentTheme, themeScheme: defaultThemeScheme } = useTheme();

    const colorVariant = () => {
        switch (variant) {
            case "outline":
                return {
                    backgroundColor: currentTheme.secondary,
                    color: currentTheme.secondary_foreground,
                    borderColor: currentTheme.secondary_foreground
                }
            case "secondary":
                return {
                    backgroundColor: currentTheme.secondary,
                    color: currentTheme.secondary_foreground,
                    borderColor: currentTheme.secondary
                }
            case "danger":
                return {
                    backgroundColor: currentTheme.destructive,
                    color: currentTheme.destructive_foreground,
                    borderColor: currentTheme.destructive
                }
            case "warning":
                return {
                    backgroundColor: "hsl(47.9 95.8% 53.1%)",
                    color: "hsl(26 83.3% 14.1%)",
                    borderColor: "hsl(47.9 95.8% 53.1%)"
                }
            case "success":
                return {
                    backgroundColor: "hsl(142.1 76.2% 36.3%)",
                    color: "hsl(355.7 100% 97.3%)",
                    borderColor: "hsl(142.1 76.2% 36.3%)"
                }
            case "default":
                return {
                    backgroundColor: currentTheme.primary,
                    color: currentTheme.primary_foreground,
                    borderColor: currentTheme.border,
                }
            default:
                const theme = themeColors.find((theme) => theme.name === variant)![themeScheme ?? defaultThemeScheme]
                if (theme) {
                    return {
                        backgroundColor: theme.primary,
                        color: theme.primary_foreground,
                        borderColor: theme.border,
                    }
                }
                return {
                    backgroundColor: currentTheme.primary,
                    color: currentTheme.primary_foreground,
                    borderColor: currentTheme.border,
                }

        }
    }

    const sizeVariant = () => {
        switch (size) {
            case "medium":
                return {
                    paddingVertical: 10,
                    paddingHorizontal: 24,
                    borderRadius: 12
                }
            case "large":
                return {
                    paddingVertical: 10,
                    paddingHorizontal: 42,
                    borderRadius: 14,
                }
            case "extraLarge":
                return {
                    paddingVertical: 10,
                    paddingHorizontal: 50,
                    borderRadius: 12,
                }
            default:
                return {
                    paddingVertical: 10,
                    paddingHorizontal: 18,
                    borderRadius: 12,
                }
        }
    }

    const textStyleVariant = () => {
        switch (size) {
            case "medium":
                return {
                    fontSize: 14,
                }
            case "large":
                return {
                    fontSize: 16
                }
            case "extraLarge":
                return {
                    fontSize: 18
                }
            default:
                return {
                    fontSize: 14
                }
        }
    }

    const buttonStyle = {
        ...colorVariant(),
        ...sizeVariant(),
    };

    if (!currentTheme) return <></>

    return (
        <TouchableOpacity
            activeOpacity={0.6}
            disabled={disabled}
            style={[{
                alignItems: 'center',
                justifyContent: 'center',
                elevation: 0.4,
                flexDirection: 'row',
                gap: 5,
                opacity: disabled ? 0.6 : 1,
                borderWidth: 0.6,
                flexWrap: 'wrap',
            },
                style,
                buttonStyle,
            ]}
            {...otherProps}>
            <ButtonContent children={children}
                textStyle={[textStyle, textStyleVariant()]}
                icon={icon}
                textTextProps={textTextProps}
                loadingStyle={loadingStyle}
                loadingProps={loadingProps}
                loading={loading}
                buttonStyle={buttonStyle} />
        </TouchableOpacity>
    )
})

export default Button

const ButtonContent = ({
    children,
    textStyle,
    icon,
    loading,
    buttonStyle,
    loadingStyle,
    loadingProps,
    textTextProps
}: {
    children: string | React.ReactNode,
    icon: React.ReactNode,
    loading: boolean,
    textTextProps?: TextProps,
    textStyle: TextProps["style"],
    loadingProps?: ActivityIndicatorProps,
    loadingStyle: ActivityIndicatorProps["style"],
    buttonStyle: {
        backgroundColor: string,
        color: string,
        borderColor: string,
        paddingVertical: number,
        paddingHorizontal: number,
        borderRadius: number
    }
}) => {
    if (loading) return <ActivityIndicator color={buttonStyle.color} style={[loadingStyle]} {...loadingProps} />

    if (typeof children === "string") {
        return <>
            {icon ? icon : <></>}
            <Text
                {...textTextProps}
                numberOfLines={1}
                style={[{
                    color: buttonStyle.color,
                    textAlign: 'center',
                    textAlignVertical: 'center',
                    fontSize: 16,
                    fontWeight: "700",
                }, textStyle]}>
                {children}
            </Text>
        </>
    }

    else if (typeof children === "object") {
        //@ts-ignore
        return children.map((child, index) => {
            if (typeof child === "string") {
                return <Text key={index} style={[{
                    color: buttonStyle.color,
                    textAlign: 'center',
                    textAlignVertical: 'center',
                    fontSize: 16,
                    fontWeight: "700",
                }, textStyle]}>
                    {child}
                </Text>
            }
            else {
                return child
            }
        })
    }
    return (
        <>
            {children}
        </>
    )
}`,
            label: 'React Native',
            language: 'tsx'
        },
    ]}
    copyButton
/>

---

### **Conclusion**

The `Button` component is a flexible and theme-aware component for React Native applications. It supports sizes, variants, loading states, and icons while ensuring a consistent and customizable design experience.
